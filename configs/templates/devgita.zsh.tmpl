# ============================================================================
# Devgita Shell Configuration
# Auto-generated from template - DO NOT EDIT MANUALLY
# ============================================================================

# Core Tools
# ----------------------------------------------------------------------------
{{if .Mise}}
# Mise - Polyglot runtime manager
if command -v mise &> /dev/null; then
  eval "$(mise activate zsh)"
fi
{{end}}

{{if .Zoxide}}
# Zoxide - Smarter cd command
if command -v zoxide &> /dev/null; then
  eval "$(zoxide init zsh)"
fi
{{end}}

# Shell Enhancements
# ----------------------------------------------------------------------------
{{if .ZshAutosuggestions}}
# Zsh Autosuggestions - Fish-like autosuggestions
{{if .IsMac}}
if [[ -f $(brew --prefix)/share/zsh-autosuggestions/zsh-autosuggestions.zsh ]]; then
    source $(brew --prefix)/share/zsh-autosuggestions/zsh-autosuggestions.zsh
fi
{{else}}
if [[ -f /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh ]]; then
    source /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh
fi
{{end}}
{{end}}

{{if .ZshSyntaxHighlighting}}
# Zsh Syntax Highlighting - Fish-like syntax highlighting
{{if .IsMac}}
if [[ -f $(brew --prefix)/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh ]]; then
    source $(brew --prefix)/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
fi
{{else}}
if [[ -f /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh ]]; then
    source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
fi
{{end}}
{{end}}

# Theme
# ----------------------------------------------------------------------------
{{if .Powerlevel10k}}
# Powerlevel10k - Fast and flexible Zsh theme
{{if .IsMac}}
if [[ -f $(brew --prefix)/share/powerlevel10k/powerlevel10k.zsh-theme ]]; then
    source $(brew --prefix)/share/powerlevel10k/powerlevel10k.zsh-theme
fi
{{else}}
if [[ -f ~/powerlevel10k/powerlevel10k.zsh-theme ]]; then
    source ~/powerlevel10k/powerlevel10k.zsh-theme
elif [[ -f /usr/share/powerlevel10k/powerlevel10k.zsh-theme ]]; then
    source /usr/share/powerlevel10k/powerlevel10k.zsh-theme
fi
{{end}}
{{end}}

# ============================================================================
# Devgita Functions & Aliases
# ============================================================================
{{if .ExtendedCapabilities}}
# Extended capabilities
dge() {
  local allowed=(
    # git branch management
    "delete-branch"
    "refresh-branch"
    "reset-main-branch"
    # npm dependency management
    "reinstall-libraries"
    "reinstall-library"
    # github pr management
    "fetch-pr-comments"
  )

  local cmd="$1"
  local target="$2"

  # Validate command
  local is_valid=false
  for a in "${allowed[@]}"; do
    if [[ "$cmd" == "$a" ]]; then
      is_valid=true
      break
    fi
  done

  if [[ "$is_valid" == false ]]; then
    echo "valid commands are: ${allowed[*]}"
    return 1
  fi

  case "$cmd" in
    # -------------------------------------
    # Git Branch Management 
    # -------------------------------------
    {{if .Fzf}}
    delete-branch)
      # Default to "main" if not provided
      target="${target:-main}"

      git checkout "$target" \
        && git fetch origin \
        && git pull origin "$target" \
        && git branch | fzf-tmux -p 50% --reverse | xargs git branch -D
      ;;
    {{end}}

    refresh-branch)
      # Default to "main" if not provided
      target="${target:-main}"

      git checkout "$target" \
        && git pull origin "$target" \
        && git checkout - \
        && git merge "$target"
      ;;

    reset-main-branch)
      git checkout main && git reset --hard origin/main
      ;;

    # -------------------------------------
    # NPM Dependency Management
    # -------------------------------------
    reinstall-libraries)
      git clean -Xdf \
        && rm -rf node_modules/ \
        && npm install \
        && rm -f tsconfig.tsbuildinfo
      ;;

    reinstall-library)
      # Must provide target
      if [[ -z "$target" ]]; then
        echo "update-library requires a library name"
        return 1
      fi

      rm -rf "node_modules/$target" && npm install
      ;;

    # -------------------------------------
    # GitHub PR Management
    # -------------------------------------
    fetch-pr-comments)
      local repo="$target"
      local pr_number="$3"
      local output_file="${4:-pr_comments.json}"

      [[ -z "$repo" || -z "$pr_number" ]] && echo "Usage: dge fetch-pr-comments <REPO> <PR_NUMBER>" && return 1
      ! command -v gh &>/dev/null && echo "Error: gh not installed" && return 1
      ! command -v jq &>/dev/null && echo "Error: jq not installed" && return 1

      local owner="${repo%/*}"
      local name="${repo#*/}"

      gh api graphql -f query='
        query($owner: String!, $name: String!, $pr: Int!) {
          repository(owner: $owner, name: $name) {
            pullRequest(number: $pr) {
              reviewThreads(first: 100) {
                nodes {
                  isResolved
                  isOutdated
                  comments(first: 100) {
                    nodes {
                      id
                      path
                      line
                      body
                      author { login }
                      createdAt
                    }
                  }
                }
              }
            }
          }
        }
      ' -f owner="$owner" -f name="$name" -F pr="$pr_number" 2>/dev/null | jq '
        [
          .data.repository.pullRequest.reviewThreads.nodes[] 
          | . as $thread
          | .comments.nodes[0]
          | select(.body | test("^https?://[^\\s]+$") | not)
          | {
              path,
              line,
              body,
              author: .author.login,
              is_resolved: $thread.isResolved,
            }
        ]
      ' > "$output_file" && echo "✓ Saved to $output_file"
      ;;

  esac
}
{{end}}

{{if .Fzf}}
# File system
alias ff="fzf --preview 'bat --style=numbers --color=always {}'"
{{end}}

# quick edits
alias edit-zsh="nvim ~/.zshrc"
alias edit-ohmyzsh="nvim ~/.oh-my-zsh"
alias edit-tmux="nvim ~/.tmux.conf"
alias edit-nvim="nvim ~/.config/nvim/init.lua"

# Directories
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# Tools
{{if .LazyGit}}
alias lzg='lazygit'
{{end}}
{{if .LazyDocker}}
alias lzd='lazydocker'
{{end}}

{{if .Neovim}}
# Neovim
alias n='nvim'
{{if .Fzf}}
alias ns="fd --type f --hidden --exclude .git | fzf-tmux -p  --reverse | xargs nvim"
{{end}}
{{end}}

# Git
alias g='git'
alias gcm='git commit -m'
alias gcam='git commit -a -m'
alias gcad='git commit -a --amend'

{{if .Tmux}}
# Tmux
alias tml="tmux list-sessions"
alias tmk="tmux kill-session -t"
alias tms="tmux switch-client -t"
alias tmkall="tmux kill-server"
alias tmclear="clear && tmux clear-history && clear"
tmn() {
    local session_name="$1"
    local path="${2:-$HOME}"
    /usr/local/bin/tmux new-session -d -s "$session_name" -c "$path"
    /usr/local/bin/tmux switch-client -t "$session_name"
}
{{end}}

# Compression
compress() { tar -czf "${1%/}.tar.gz" "${1%/}"; }
alias decompress="tar -xzf"

{{if .Mise}}
# Mise
alias mx="mise x --"
{{end}}

{{if .Zoxide}}
# ---- Zoxide (better cd) ----
alias cd="z"
{{end}}

{{if .Eza}}
# ---- Eza (better ls) ----
alias ls='eza -lh --group-directories-first --icons'
alias lsa='ls -a'
alias lt='eza --tree --level=2 --long --icons --git'
alias lta='lt -a'
{{end}}

{{if .Eza}}
# ---- Bat (better cat) ----
alias cat="bat"
{{end}}

{{if .Opencode}}
alias oc=opencode
{{end}}

{{if .Claudecode}}
alias cc="CLAUDE_CODE_NO_FLICKER=1 claude"
{{end}}
# ============================================================================
# End of Devgita Configuration
# ============================================================================
