[env]
TERM = "xterm-256color"

[terminal.shell]
program = "<ALACRITTY-CONFIG-PATH>/alacritty/program.sh"

[general]
import = ["~/.config/alacritty/theme.toml", "~/.config/alacritty/font.toml", "~/.config/alacritty/font-size.toml" ]

[window]
padding.x = 10
padding.y = 10

decorations = "Buttonless"

opacity = 0.8
blur = true

[terminal]
